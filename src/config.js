//全局的设置， 会应用到之后所有的实例中
export default {
  mixins: [],
  defaultTextDelimiters: ['${', '}'],
  defaultDirectiveAttributeDelimiters: ['[', ']'],
  defaultEventAttributeDelimiters: ['(', ')'],
  async: true,
}